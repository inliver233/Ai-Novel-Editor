# 完全重构计划：AI Novel Editor（Ai-Novel-Editor / UltraWork）

## Context

### Original Request（用户原话意图归纳）
- 对整个项目进行一次“细致到每一行代码”的全面探索，摸清架构、核心逻辑与所有问题。
- 列出冗余/垃圾/BUG/失败功能/堆砌实现，并制定“事无巨细”的**完全重构计划**：
  - 项目分块 + 架构设计 + UI 设计 + 代码规范 + 重构步骤 + 各功能实现与迁移策略。

### 现状总览（调研结论，已确认）

**技术栈**
- Python 3.8+（仓库内存在 cp39/cp310 的 `*.pyc`）
- PyQt6 桌面应用
- SQLite：项目库 `project.db`（文档树 + Codex），向量库 `vectors.db`（RAG embeddings）
- AI：OpenAI/Claude/Gemini/Ollama/Custom（OpenAI 兼容）
- NLP：jieba、nltk、numpy

**入口链路**
- `src/main.py`：初始化日志、QApplication、Config/Shared/ProjectManager；尝试初始化 RAG 与 Codex；最后启动 `MainWindow`。
- `src/gui/main_window.py`：三栏布局（左项目树/中编辑器/右大纲+Codex），并负责“把所有东西粘起来”。

**仓库规模**
- git 跟踪文件总数：269
- Python 源文件：146
- 严重卫生问题：git 跟踪了 115 个 `*.pyc`（`src/**/__pycache__/*.pyc`）。

**热点（巨型文件 Top）**
- `src/gui/panels/outline_panel.py`（2577 行）
- `src/core/codex_manager.py`（2201 行）
- `src/gui/main_window.py`（2063 行）
- `src/gui/ai/unified_ai_config_dialog.py`（1925 行）
- `src/gui/panels/codex_panel.py`（1662 行）
- `src/gui/ai/enhanced_ai_manager.py`（1475 行）
- `src/core/ai_client.py`（1386 行）
- `src/core/import_export_engine.py`（1281 行）
- `src/core/sqlite_vector_store.py`（1096 行）
- `src/core/rag_service.py`（1044 行）
- `src/gui/editor/smart_completion_manager.py`（927 行）
- `src/gui/editor/text_editor.py`（903 行）
- `src/core/prompt_functions.py`（900 行）

**量化风险信号**
- `src/**/*.py` 中 `except Exception`：352 处；`bare except:` 26 处（隐藏异常/吞错风险高）。
- `print(`：117 处（GUI 对话框/分析输出混入生产路径的风险）。

### 核心“精髓”（重构必须守住的主干）

把项目当作 5 条主干能力：
1) **Project（项目/文档树）**：Act/Chapter/Scene 等层次结构 + 文档编辑 + 保存/加载。
2) **Editor（写作编辑器）**：智能补全入口（键盘处理、ghost text、inline completion）、自动替换、统计信息。
3) **AI Pipeline（提示词与补全）**：上下文收集 → prompt 生成 → LLM 请求（多 provider）→ 流式/非流式输出 → UI 展示与应用。
4) **Codex（知识库）**：角色/地点/物品等概念管理 + 引用追踪。
5) **RAG（检索增强）**：索引构建（chunk/embedding）+ 查询（vector/keyword fallback）+ 重排（rerank）。

### Metis Review（关键缺口与护栏）

**必须显式写进计划的护栏**
- 默认不做“UX 大改”（交互/工作流不破坏），UI 重构以“视觉一致 + 组件收敛 + 性能/可用性”优先。
- 默认保持 `project.db`/`vectors.db` 兼容；任何 schema 变更必须：备份 + 迁移 + 回滚/恢复方案。
- 严禁把网络/embedding 等重操作放 UI 线程；明确任务取消、并发与节流策略。
- 新依赖要克制：优先 dev-only（ruff/black/mypy/pytest-qt），运行时依赖慎加。

**隐性风险**
- 数据损坏：双库漂移（project ↔ vectors）、embedding model 改动导致维度/语义不兼容。
- 隐私：日志/print 泄露提示词/正文；提示词注入（RAG 文档内容进入 prompt）。
- 打包：Qt 插件、jieba 词典、weasyprint 等系统依赖在 PyInstaller/Nuitka 下的差异。

---

## Work Objectives

### Core Objective
在不破坏现有核心工作流与用户数据的前提下，把当前“屎山”重构为：**边界清晰、可测试、可演进、可打包发布**的 PyQt6 桌面应用。

### Concrete Deliverables
- 代码库分层与模块边界（core/domain/application/gui 分离），并逐步拆解巨型文件。
- 可重复的开发与验证流程：一键运行、测试、打包（至少 Windows）。
- AI/RAG/Codex/Project/Editor 五条主干的契约化接口与可测试替身（mock/fake）。
- UI 设计系统（QSS tokens + 组件库）与关键页面一致化。
- 仓库卫生：移除已跟踪 `*.pyc`、收敛 archive/temp_research 噪音。

### Definition of Done（最终验收）
- [ ] `python src/main.py` 可启动（无阻塞卡死），核心流程可用。
- [ ] `python -m unittest discover tests` 通过（或迁移到 `pytest` 后 `pytest` 通过）。
- [ ] 具备 GUI smoke test（最少：启动 → 打开项目 → 打开文档 → 触发 AI（mock）→ 保存 → 退出）。
- [ ] 真实项目数据（至少 1 个大项目）可无损打开/保存/重开；Codex 引用/索引不丢。
- [ ] 性能预算：启动、打开项目、刷新大纲、触发补全不出现明显 UI 冻结（定义见后）。

### Must Have
- 数据安全（备份/迁移/恢复）优先级最高。
- 明确层次边界：数据层/业务层/GUI 层/外部服务层。
- 任务系统（后台线程、取消、节流、错误展示）标准化。

### Must NOT Have（防止重构“写爽了”）
- 不做“全量重写到 Web/Electron/Tauri”（除非后续明确选择）。
- 不引入大量运行时依赖（除非证明收益大且打包可控）。
- 不把复杂策略塞进错误层（例如 VectorStore 里做 AI 关键词提取）。

---

## Verification Strategy（测试与验收策略）

### Test Decision
- **Infrastructure exists**: YES（`tests/` 目录 + requirements 中 pytest）
- **Plan default**: Tests-after + 针对高风险模块局部 TDD
- **Framework**: 现有 unittest 保持可跑；逐步引入 pytest + pytest-qt 作为新测试入口

### Why
- 全量 TDD 对 2k+ 行巨型模块成本过高；更现实的是：先建立 smoke/契约测试，随后用“分段拆解 + 回归用例”推进。

### 必须增加的验证层（重构安全网）
- **Smoke（GUI）**：pytest-qt 启动窗口 + 关键交互（不要求真实 API）。
- **Golden Master（行为回归）**：对“提示词构建、上下文抽取、RAG 搜索 topN、Codex 引用检测”建立固定输入输出用例。
- **Data Safety**：项目数据库迁移/备份/恢复的自动化用例。

---

## Target Architecture（目标架构设计）

### 分层原则（依赖方向）
目标是让依赖只从外向内：
- `gui/`（PyQt Widgets） → `application/`（用例/编排） → `domain/`（纯数据与规则）
- `infrastructure/`（SQLite/HTTP/FS/KeyStore）只通过接口被 `application/` 调用

**核心规则**
- `domain/` 禁止 import PyQt6。
- Qt 线程/信号属于 `gui/` 或 `gui/adapters/`。

### 建议的目录重组（渐进式，Strangler Fig）
第一阶段不要求一次性移动所有代码；先建立新目录并把新代码放进去，再逐步迁移旧代码。

**Decision（Phase 1 写死）**
- Phase 1 不更换入口：继续以 `src/main.py` 作为启动入口与打包入口（避免影响资源路径/打包脚本/用户习惯）。
- `src/app/main.py` 仅作为 Phase 2+ 的可选重构目标：当资源加载、依赖注入与打包脚本稳定后再引入。

建议目标形态：
```
src/
  app/
    main.py                 # 新入口（可选：python -m ai_novel_editor）
  domain/
    project/                # ProjectData/ProjectDocument/类型
    codex/                  # CodexEntry/CodexReference/规则
    ai/                     # 纯接口与数据结构（不含网络）
  application/
    services/               # ProjectService, CodexService, AICompletionService, IndexService...
    tasks/                  # TaskManager 抽象（取消/进度/节流）
  infrastructure/
    sqlite/                 # project.db repos, schema migrations
    vectors/                # vectors.db store
    llm/                    # provider adapters (OpenAI/Claude/Gemini/Ollama)
    secrets/                # secure_key_manager (OS keychain optional)
  gui/
    main_window/            # 拆分 main_window 巨文件
    editor/
    panels/
    dialogs/
    themes/
    components/             # 统一组件库
```

### 数据层策略（默认）
- **默认保持双库**：`project.db`（项目+Codex） 与 `vectors.db`（embeddings）分离。
- **默认每项目一份 vectors.db（推荐）**：例如 `<project_path>/.rag/vectors.db`，避免跨项目混用/泄露，并提升便携性。
- 在 `project.db` 里新增/强化“schema version + migration history + backup marker”。
- 在 `vectors.db` 里明确 embedding_model、维度、chunking 参数版本号；做到“可检测不兼容并自动重建”。

### 任务与并发模型（默认）

**Decision（写死，避免执行期分叉）**
- 默认采用 `QThreadPool + QRunnable` 承载后台任务（避免“每个功能一套线程模型”）。
- Worker 线程只做计算/IO；所有 UI 更新通过信号回到主线程。

**TaskManager v1 最小规格（写死）**
- 职责：提交任务、按 key 去重/合并、取消、节流/去抖、进度与错误上报。
- 关键行为：
  - `submit(key, work_fn, on_progress, on_done, on_error, cancel_token)`
  - `cancel(key)`：取消某 key 的在途任务（新的 submit 同 key 也应先 cancel 再替换）
  - `debounce(key, delay_ms, submit_args...)`：用于 documentSaved/index/reference detect
  - `throttle(key, interval_ms, submit_args...)`：用于高频 UI 触发
  - `cancel_all(scope)`：切换项目/退出时批量取消
- `cancel_token` 语义（写死，给 QRunnable 一个“能取消”的最小约定）：
  - 最小接口：
    ```python
    class CancelToken:
        def __init__(self):
            self._cancelled = False
        def cancel(self):
            self._cancelled = True
        def cancelled(self) -> bool:
            return self._cancelled
    ```
  - Worker 约定：
    - 启动前检查一次 `token.cancelled()`
    - 关键循环/批处理每处理 N 项检查一次（例如每 10 个 chunk）
    - 在写 DB/发 UI 信号前再检查一次（确保取消后不会写入/不会更新 UI）
- 错误路径（写死）：
  - 所有异常必须：记录到日志 + 上报到 GUI（StatusBar / 通知中心）
  - 禁止静默吞掉异常（除非明确标注为“可忽略”并记录 debug）

**验收思路（写死）**
- 任一重任务（索引/导入/重建）在 UI 中必须：可取消 + 有进度/状态提示 + 取消后不再继续写入 DB。

### AI Pipeline 目标形态
- `AIClient` 拆分成 provider 策略（OpenAI/Claude/Gemini/Ollama/Custom），共同实现统一接口。
- `PromptComposer`（提示词构建）与 `ContextCollector`（上下文收集）独立、可测试。
- 明确“可控开关”：
  - 是否启用 RAG
  - 是否启用 Codex 全局注入
  - 自动触发/手动触发/禁用

### RAG 目标形态
- `VectorStore` 必须“纯存储/纯检索”，不做 UI、不做网络、不做 AI 关键词提取。
- `RAGQueryPlanner`/`KeywordExtractor` 放在 application 层（可选使用 jieba/规则/AI）。
- 性能：对大项目提供可扩展索引（可选 sqlite-vss/FAISS），但默认可继续用现有 SQLite+numpy。

---

## UI Design（UI 设计与组件体系）

### 设计目标
- 保留三栏结构与主要功能入口，不改变用户工作流。
- 收敛“现代 UI”表达：统一 spacing、字体、圆角、阴影、交互反馈（hover/pressed/focus）。
- 把 AI 状态/控制从“散落各处”收敛成一个一致的“智能中心”。

### 设计系统（QSS Tokens）
- 在现有 `src/resources/styles/*.qss` 基础上建立 tokens：
  - 颜色：bg/text/border/semantic/ai
  - 间距：4/8/12/16/24/32
  - 字体：正文/标题/等宽（写作区）
  - 组件半径：6/10/14
  - 动效：100/180/260ms（Qt 动画）

### 组件收敛（候选）
- `gui/components/`：
  - `StatusBadge`（统一 AI/RAG/Codex 状态显示）
  - `SidebarSectionHeader`（面板标题）
  - `SearchBox`（统一查找框样式/行为）
  - `Card`（Codex 卡片/统计卡片）
  - `EmptyState`（无内容/未启用提示）

### 工作区预设（不改变核心逻辑，只是布局 preset）
- 写作模式：隐藏右侧面板/保留 AI 工具。
- 大纲模式：右侧大纲最大化。
- 世界观模式：Codex 最大化。

---

## Engineering Standards（代码规范与工程化）

> 这部分是“完全重构”的地基：没有一致的规范与质量门禁，后续拆分会持续回归。

### 语言与运行时基线（默认）
- **默认 Python 基线**：3.10（与仓库中 cp310 产物一致），但尽量不无意抬高到 3.11+。
- 如必须支持 3.8：需要在计划执行阶段显式评估依赖（PyQt6、weasyprint、cryptography 等）兼容矩阵。

### 格式化/静态检查（建议组合）
- `ruff`：Lint + import 规范 + 部分自动修复
- `black`：格式化（降低 diff 噪音）
- `mypy`：分阶段启用（从 core/domain 先做“有益类型”，再扩散）
- `pre-commit`：把 ruff/black/mypy/pytest 作为本地门禁

### 代码规则（必须写死）
- 禁止 `bare except:`，除非同时：记录错误 + 注释说明原因 + 只在极窄范围兜底。
- 禁止用 `print()` 做日志（全部换 `logging`，并通过 `src/core/log.py` 统一配置）。
- 禁止在 UI 线程做：网络请求、embedding、全量大纲重建、全库扫描。
- “core/domain” 不 import PyQt6（短期过渡允许，但必须有迁移任务和截止点）。

### 隐私与安全（默认策略）
- API Key 一律不落盘明文；日志中必须脱敏（写死为“统一在日志系统做过滤”，避免各模块各自处理）。
  - 统一落点（Phase 1 写死）：在 `src/core/log.py:117-166`（`configure_logging()`）增加 redaction filter / formatter，对日志消息做模式替换。
  - 覆盖模式（写死）：
    - JSON 中的 `"api_key": "..."` / `api_key=`
    - OpenAI 风格 key：`sk-...`
    - Header 风格：`Bearer ...` / `x-api-key: ...` / `x-goog-api-key: ...`
  - 示例（写死）：
    - 输入：`RAG配置: {'api_key': 'sk-xxx', 'base_url': '...'}'
    - 输出：`RAG配置: {'api_key': '<redacted>', 'base_url': '...'}'
- Prompt 注入：对来自文档/RAG/Codex 的内容做“边界标注 + 角色隔离 + 工具调用开关”。

---

## Current Module Inventory（现有模块清单与处理策略）

> 目标：让执行者拿到清单即可定位改动点；并为“逐文件拆解”提供导航。

### src/core（当前文件列表）
- `src/core/ai_client.py`：多 provider LLM 客户端（需拆分策略）。
- `src/core/ai_qt_client.py`：Qt 异步桥接（建议迁移到 gui/adapters）。
- `src/core/project.py`：ProjectManager + 数据模型 +（不应存在的）QThread 索引逻辑。
- `src/core/database_manager.py`：project.db schema + CRUD + migrations。
- `src/core/codex_manager.py`：Codex 数据/缓存/正则引用检测 + Qt 信号（需去 Qt 化）。
- `src/core/rag_service.py`：RAG 编排（网络、重试、fallback）。
- `src/core/sqlite_vector_store.py`：向量存储与检索（需“纯存储化”）。
- `src/core/import_export_engine.py`：导入导出引擎（需拆分格式插件）。
- `src/core/prompt_functions.py`：提示词函数集合（需拆成 package）。
- 其余：segmentation、context extractor、outline parser/enhancer、tool_manager 等。

完整清单（git 跟踪）：
```
src/core/__init__.py
src/core/ai_client.py
src/core/ai_qt_client.py
src/core/auto_replace.py
src/core/chinese_segmentation.py
src/core/codex_manager.py
src/core/concurrent_io.py
src/core/config.py
src/core/database_manager.py
src/core/export_manager.py
src/core/import_export_engine.py
src/core/import_manager.py
src/core/improved_context_extractor.py
src/core/intelligent_context_collector.py
src/core/log.py
src/core/multimodal_types.py
src/core/nlp_analyzer.py
src/core/optimized_entity_detector.py
src/core/outline_converter.py
src/core/outline_enhancer.py
src/core/outline_parser.py
src/core/outline_prompts.py
src/core/project.py
src/core/prompt_functions.py
src/core/rag_service.py
src/core/reference_detector.py
src/core/secure_key_manager.py
src/core/shared.py
src/core/simple_prompt_service.py
src/core/sqlite_vector_store.py
src/core/template_processor.py
src/core/tool_manager.py
src/core/tool_types.py
```

### src/gui（按子系统分组）

完整清单（git 跟踪，去除 __pycache__）：
```
[src/gui]
src/gui/__init__.py
src/gui/main_window.py

[src/gui/ai]
src/gui/ai/__init__.py
src/gui/ai/ai_completion_control.py
src/gui/ai/completion_widget.py
src/gui/ai/config_mapper.py
src/gui/ai/enhanced_ai_manager.py
src/gui/ai/enhanced_prompt_config_widget.py
src/gui/ai/rag_config_widget.py
src/gui/ai/simplified_prompt_dialog.py
src/gui/ai/simplified_prompt_widget.py
src/gui/ai/stream_widget.py
src/gui/ai/unified_ai_config_dialog.py

[src/gui/controllers]
src/gui/controllers/project_controller.py

[src/gui/dialogs]
src/gui/dialogs/__init__.py
src/gui/dialogs/about_dialog.py
src/gui/dialogs/alias_management_dialog.py
src/gui/dialogs/auto_replace_dialog.py
src/gui/dialogs/codex_entry_dialog.py
src/gui/dialogs/enhanced_find_dialog.py
src/gui/dialogs/export_dialog.py
src/gui/dialogs/find_replace_dialog.py
src/gui/dialogs/import_dialog.py
src/gui/dialogs/import_export_dialog.py
src/gui/dialogs/outline_analysis_dialog.py
src/gui/dialogs/progression_dialog.py
src/gui/dialogs/progression_management_dialog.py
src/gui/dialogs/project_settings_dialog.py
src/gui/dialogs/rag_index_dialog.py
src/gui/dialogs/relationship_management_dialog.py
src/gui/dialogs/settings_dialog.py
src/gui/dialogs/shortcuts_dialog.py
src/gui/dialogs/simple_find_dialog.py
src/gui/dialogs/word_count_dialog.py

[src/gui/editor]
src/gui/editor/__init__.py
src/gui/editor/codex_highlighter.py
src/gui/editor/completion_engine.py
src/gui/editor/completion_status_indicator.py
src/gui/editor/completion_widget.py
src/gui/editor/deep_integrated_ghost_text.py
src/gui/editor/editor_panel.py
src/gui/editor/focus_mode.py
src/gui/editor/inline_completion.py
src/gui/editor/modern_ai_indicator.py
src/gui/editor/optimal_ghost_text.py
src/gui/editor/smart_completion_manager.py
src/gui/editor/syntax_highlighter.py
src/gui/editor/text_editor.py
src/gui/editor/timeout_manager.py

[src/gui/menus]
src/gui/menus/__init__.py
src/gui/menus/menu_bar.py
src/gui/menus/toolbar.py

[src/gui/panels]
src/gui/panels/__init__.py
src/gui/panels/codex_panel.py
src/gui/panels/codex_reference_stats.py
src/gui/panels/enhanced_reference_stats.py
src/gui/panels/modern_codex_card.py
src/gui/panels/outline_panel.py
src/gui/panels/project_panel.py
src/gui/panels/relationship_graph.py

[src/gui/status]
src/gui/status/__init__.py
src/gui/status/status_bar.py

[src/gui/themes]
src/gui/themes/__init__.py
src/gui/themes/theme_manager.py
```

（注：`src/gui/ai/completion_widget.py` 与 `src/gui/editor/completion_widget.py` 同名，长期会制造导入/认知冲突，建议重命名或统一。）

---

## Feature Map（功能 → 代码位置 → 重构方向）

> 这部分把“用户感知的功能”与“代码落点”对齐，避免重构过程中遗漏或误删。

### 项目管理（创建/打开/保存/多项目/文档树）
- **代码位置**:
  - `src/core/project.py`（ProjectManager + 数据模型）
  - `src/core/database_manager.py`（project.db 持久化）
  - `src/gui/panels/project_panel.py`（左侧项目树 UI）
  - `src/gui/controllers/project_controller.py`（UI → project 操作编排）
- **问题画像**:
  - core 中出现 Qt 线程逻辑（索引），边界破坏；保存策略可能偏粗（全量写入/清空重插）。
- **重构方向**:
  - Project 变成 domain + repository；索引调度迁到 application/gui；提供增量更新与事务边界。

### 编辑器（写作区/快捷键/自动替换/统计）
- **代码位置**:
  - `src/gui/editor/text_editor.py`（核心输入/渲染）
  - `src/gui/editor/editor_panel.py`（多文档切换容器）
  - `src/core/auto_replace.py` + `src/gui/dialogs/auto_replace_dialog.py`
- **问题画像**:
  - 键盘处理 + AI 展示/状态耦合重；多个 ghost/inline 系统并存。
- **重构方向**:
  - 抽离“补全状态机”与“渲染器”；编辑器只关心渲染与输入事件。

### AI 补全（触发模式/上下文模式/流式响应）
- **代码位置**:
  - `src/gui/editor/smart_completion_manager.py`（补全入口/状态）
  - `src/gui/ai/enhanced_ai_manager.py`（上下文/提示词/调用编排）
  - `src/core/ai_client.py`（provider 客户端）
  - `src/gui/ai/stream_widget.py`（流式 UI）
- **问题画像**:
  - 职责混杂；异常吞掉；状态反馈散落；难测试。
- **重构方向**:
  - provider 策略化 + prompt/context 独立可测 + 统一任务/取消/重试策略。

### AI 配置中心（多服务商/连接测试/导入导出）
- **代码位置**:
  - `src/core/config.py`（config.json + QSettings）
  - `src/core/secure_key_manager.py`（密钥）
  - `src/gui/ai/unified_ai_config_dialog.py`（配置 UI）
  - `src/gui/ai/config_mapper.py`（UI ↔ config 映射）
- **问题画像**:
  - 配置与密钥的边界需要更强（脱敏/导出策略）；UI 巨文件。
- **重构方向**:
  - `ConfigSchema` 明确字段；密钥永不导出明文；UI 拆分为 tabs 子组件。

### 提示词与模板（内置模板/自定义/函数扩展）
- **代码位置**:
  - `src/core/simple_prompt_service.py`
  - `src/core/template_processor.py`
  - `src/core/prompt_functions.py`
  - `src/gui/ai/simplified_prompt_dialog.py` / `src/gui/ai/simplified_prompt_widget.py`
- **问题画像**:
  - prompt_functions 单文件过大；函数注册/执行与 UI 交织风险。
- **重构方向**:
  - 拆为 `prompts/functions/*` 包；建立单元测试与文档；默认禁用高风险函数。

### 大纲管理（树/拖拽/统计/预览）
- **代码位置**:
  - `src/gui/panels/outline_panel.py`（超级巨文件）
  - `src/core/outline_parser.py` / `src/core/outline_enhancer.py` / `src/core/outline_converter.py`
- **问题画像**:
  - 全量刷新/异常吞掉/性能压力；边界与 project 交互复杂。
- **重构方向**:
  - 拆为子组件；引入增量刷新；建立性能预算与 profiling。

### Codex（知识库/引用追踪/关系图）
- **代码位置**:
  - `src/core/codex_manager.py`
  - `src/core/reference_detector.py`
  - `src/gui/panels/codex_panel.py`
  - `src/gui/panels/relationship_graph.py`
- **问题画像**:
  - core 里混 Qt；缓存同步风险；引用检测与 UI 刷新耦合。
- **重构方向**:
  - domain + repo + adapter；引用检测节流；关系图可选加载。

### 搜索/替换（全项目/正则/批量）
- **代码位置**:
  - `src/gui/dialogs/simple_find_dialog.py`
  - `src/gui/dialogs/enhanced_find_dialog.py`
  - `src/gui/dialogs/find_replace_dialog.py`
- **问题画像**:
  - print 输出；可能存在重复实现。
- **重构方向**:
  - 统一为一个 Find/Replace 模块，dialog 只负责 UI。

### 导入/导出（TEXT/MD/DOCX/PDF/HTML）
- **代码位置**:
  - `src/core/import_export_engine.py` / `src/core/import_manager.py` / `src/core/export_manager.py`
  - `src/gui/dialogs/import_dialog.py` / `src/gui/dialogs/export_dialog.py` / `src/gui/dialogs/import_export_dialog.py`
- **问题画像**:
  - 单文件大而杂；错误处理与编码一致性风险；weasyprint 打包风险。
- **重构方向**:
  - 格式插件化 + round-trip 测试；打包时对 PDF 依赖做降级策略。

### 主题/外观（light/dark/high-contrast）
- **代码位置**:
  - `src/gui/themes/theme_manager.py`
  - `src/resources/styles/*.qss`
- **问题画像**:
  - 主题切换遍历 allWidgets 可能开销大；组件风格不一致。
- **重构方向**:
  - 建立 tokens 与组件库；主题切换尽量减少全量 polish。

---

## TODOs（完全重构执行清单）

> 说明：这是一个“单计划超长清单”。执行建议采用 Strangler Fig：先建立安全网，再从最危险的热点拆起。
> 每个任务都要求：引用当前文件/类；给出验收命令与人工检查步骤；强调不可破坏的数据兼容。

### 0. 建立可回滚的基线（强制先做）

- [ ] 0.1 仓库卫生：移除被 git 跟踪的 `*.pyc`/`__pycache__`

  **What to do**:
  - 列出所有被跟踪的 `src/**/__pycache__/*.pyc`（当前约 115 个）并从 git 中删除。
  - 保留 `.gitignore` 中对缓存的忽略规则；确保后续不会再进入 git。

  **Must NOT do**:
  - 不要删除真实源码文件；只移除缓存产物。

  **Parallelizable**: YES

  **References**:
  - `.gitignore` - 已包含忽略规则，但不会影响已跟踪文件。
  - `src/core/__pycache__/...` - 当前被跟踪的典型路径。

  **Acceptance Criteria**:
  - [ ] `git status` 不再显示 `src/**/__pycache__/*.pyc` 作为 tracked 文件。
  - [ ] 新生成的 `__pycache__` 不会出现在 `git status`。

- [ ] 0.2 文档策略：解除 `.gitignore` 对 `*.md` 的“一刀切”忽略（或新增 docs 白名单）

  **What to do**:
  - 调整 `.gitignore`：允许 `docs/` 或 `design/` 下文档被版本控制（保留根目录的噪音 md 忽略也行）。
  - 将本计划从 `.sisyphus/plans/完全重构计划.md`（规划输出）复制/同步到用户期望的位置（如根目录）由执行者决定。

  **Must NOT do**:
  - 不要把敏感信息（API key、真实作品内容）写入文档或提交。

  **Parallelizable**: YES

  **References**:
  - `.gitignore:162-165`（当前 `*.md` 忽略策略）

  **Acceptance Criteria**:
  - [ ] 可在版本库中新增并跟踪 `docs/` 下 md 文档（或按方案白名单）。

- [ ] 0.3 建立“必不破坏”Top 10 工作流清单 + 手工回归脚本

  **What to do**:
  - 记录 10 条必须不回归的用户路径（写作/项目/AI/Codex/RAG/导入导出/主题/查找替换）。
  - 为每条路径写出“可执行步骤 + 预期结果 + 截图/日志证据要求”。

  **Parallelizable**: YES

  **Acceptance Criteria**:
  - [ ] 清单进入 `docs/qa/top10-flows.md`（或同等位置）。

- [ ] 0.4 工程化门禁：引入 ruff/black/mypy/pre-commit（分阶段启用）

  **What to do**:
  - 新增 `pyproject.toml`（或等价配置）集中管理：ruff/black/mypy/pytest。
  - 引入 `pre-commit` 并配置：ruff（含 auto-fix）、black、mypy（可先对 core/domain）。
  - 制定“分阶段严格度”：
    - Phase A：只做格式化/明显错误（unused imports、语法问题）
    - Phase B：引入类型与更严格 lint（逐模块推进）

  **Must NOT do**:
  - 不要一次性让全仓库 lint 全红；会导致无法推进。

  **Parallelizable**: YES

  **References**:
  - `src/core/`（先从 core 开始启用 mypy/ruff 规则）

  **Acceptance Criteria**:
  - [ ] `pre-commit run --all-files` 可执行（允许部分规则先 warn）。
  - [ ] 新增 `docs/engineering/standards.md` 写清规则与例外。

- [ ] 0.5 清理输出噪音：移除生产路径中的 `print()` 并统一日志

  **What to do**:
  - 把以下文件中的 `print()` 替换为 `logging`（或删除无意义输出）：
    - `src/gui/dialogs/enhanced_find_dialog.py`（print 46 处）
    - `src/gui/dialogs/simple_find_dialog.py`（print 31 处）
    - `src/gui/dialogs/find_replace_dialog.py`（print 11 处）
    - `src/core/nlp_analyzer.py`（print 14 处）
    - `src/main.py`（print 1 处：图标加载失败提示，建议改为 logger.warning）

  **References**:
  - `src/core/log.py`（统一 logging 配置入口）

  **Acceptance Criteria**:
  - [ ] GUI 使用查找替换不再向 stdout 打印调试信息。
  - [ ] 日志级别可通过配置控制（debug/info）。

- [ ] 0.6 清理 archive 与 research 噪音（降低认知负担）

  **What to do**:
  - 评估 `archive/experimental/`（当前 git 跟踪 17 个文件）：
    - 若不再使用：迁移到单独分支/单独仓库或彻底移除。
    - 若要保留：确保不被主代码 import；并在 README 标注“仅历史参考”。
  - `temp_research/` 当前未跟踪：建议移到 `docs/research/` 或保持不进 repo。

  **Acceptance Criteria**:
  - [ ] 主代码 `src/` 中不存在对 `archive/` 或 `temp_research/` 的 import。

- [ ] 0.7 数据安全基线：备份/恢复机制（重构前必须可用）

  **What to do**:
  - 为每次 schema migration 或批量操作：自动创建备份（`project.db` / `vectors.db`）。
  - **备份实现方式（默认，写死）**：
    - `project.db`：使用 SQLite 官方备份 API（`sqlite3.Connection.backup`）导出一致快照。
      - 原因：当前启用 WAL（`src/core/database_manager.py:39-41`），直接复制单个 db 文件可能不一致。
    - `vectors.db`：同样使用 `sqlite3.Connection.backup`。
    - 仅当 backup API 不可用时才退回“文件复制”，且必须同时复制 `*.db` + `*.db-wal` + `*.db-shm`。
  - **备份位置（默认）**：
    - `project.db`：放在项目目录内 `backups/YYYYMMDD-HHMMSS/project.db`
    - `vectors.db`：若迁移到“每项目一份 vectors.db”（见 4.2），同样放项目目录内；若暂时保留全局库，则备份放 `~/.ai-novel-editor/backups/YYYYMMDD-HHMMSS/vectors.db`
  - **保留策略（默认，写死）**：保留最近 N=10 份（按时间戳排序，超过即删除最旧）。
  - **备份触发条件（默认，写死）**：
    - 必做：任何 schema migration（`_migrate_database()`）前。
    - 必做：用户主动触发的“危险操作”前：
      - 项目导入（可能覆盖大量文档）
      - 批量删除（删除 Act/Chapter 递归）
      - RAG 重建索引 / `clear_all()`
      - 从备份恢复（恢复前也先备份当前状态）
    - 选做：每次打开项目后创建一次“会话基线备份”（只做一次，避免每次保存都备份）。
    - **明确禁止（写死）**：普通保存路径（例如编辑器 Ctrl+S / 自动保存）不应触发自动备份。
      - 原因：当前 `DatabaseManager.save_project_data()` 采用 `DELETE FROM documents` + 全量重插（写入频繁），若每次保存都备份会导致磁盘爆炸与 UI 卡顿。
      - 如必须支持“按保存做版本”：应另做可配置的版本控制/快照系统（不属于本次默认重构范围）。
  - **备份集合（写死）**：备份以“集合”为单位，至少包含：
    - `project.db` 快照
    - `vectors.db` 快照（若 vectors 已迁移到每项目库；若仍为全局库则备份全局库）
    - `manifest.json`（记录：时间戳、app version、project_path（脱敏/相对）、project.db schema version、vectors embedding_model/维度/chunk 参数版本）
  - **接入点（必须写死）**：
    - `src/core/database_manager.py`：进入 `_migrate_database()` 前调用 backup。
    - `src/gui/dialogs/import_dialog.py` / `src/gui/dialogs/import_export_dialog.py`：执行导入前先备份。
    - `src/core/project.py`：执行递归删除（`remove_document`）前先备份（或由 controller 层统一备份）。
    - `src/core/sqlite_vector_store.py`：执行 `clear_all()` 或“重建索引”前备份。
  - 提供恢复入口（GUI 菜单或 CLI 工具）：
    - GUI：设置/项目菜单中提供“从备份恢复”
    - CLI：`python -m tools.restore_backup --project <path> --backup <timestamp>`（执行阶段实现）

  - **BackupService v1（写死，给执行者一个可直接开工的接口面）**：
    - Phase 1 落点（写死）：`src/core/backup_service.py`（先放 core，后续再迁移到 infrastructure；先保证可用与一致性）
    - 最小 API（写死）：
      - `create_backup_set(project_path: Path, db_paths: list[Path], reason: str) -> Path`（返回 backup_dir）
      - `restore_backup_set(project_path: Path, backup_dir: Path) -> None`
      - `list_backup_sets(project_path: Path) -> list[dict]`（用于 GUI 列表展示）
    - 失败策略（写死，默认保守）：
      - migration/导入/clear_all/rebuild 等“危险操作”开始前若备份失败：默认阻止该操作并提示用户（允许高级开关跳过，但默认禁用）。

  - `manifest.json` 示例（写死，便于实现与验收）：
    ```json
    {
      "created_at": "2026-01-27T18:05:00Z",
      "reason": "before_migration",
      "app_version": "0.1.0",
      "project": {
        "path_hint": "<redacted>",
        "project_db_schema": "schema_v2"
      },
      "vectors": {
        "mode": "per_project",
        "embedding_model": "BAAI/bge-large-zh-v1.5",
        "dimension": 1024,
        "chunk": {"size": 800, "overlap": 200}
      }
    }
    ```

  **References**:
  - `src/core/database_manager.py:39-41`（WAL 模式，解释为何需要 backup API）
  - `src/core/database_manager.py:65-121`（`_migrate_database()`，schema migration 触发点）
  - `src/core/database_manager.py:203-239`（`save_project_data()` 的全量重写策略，说明为何不能每次保存都备份）
  - `src/core/sqlite_vector_store.py`（vectors.db）
  - `src/main.py:94-120`（当前 vectors.db 全局路径与 rag_config 日志风险，后续会改成脱敏 + 每项目路径）

  **Acceptance Criteria**:
  - [ ] 可以从备份恢复并成功打开项目（project.db）与检索（vectors.db）。
  - [ ] 恢复后 `python src/main.py` 可启动，且能打开该项目。
  - [ ] 备份文件可通过 `sqlite3` 的 `PRAGMA integrity_check;` 验证一致性（PASS）。
  - [ ] 普通保存 20 次（Ctrl+S / 自动保存）不会产生新的备份集合（避免误触发）。
  - [ ] 触发一次 schema migration 与一次 vectors 清空/重建前，会各自生成 1 份备份集合（可在备份目录看到 `manifest.json`）。
  - [ ] migration 的判定规则明确且不误触发：仅当 `current_version < target_version` 时才生成“migration 前备份”（见 `src/core/database_manager.py:67-70` 的现状逻辑）。

- [ ] 0.8 任务系统定稿：TaskManager v1（统一取消/去重/节流/错误上报）

  **What to do**:
  - 在 `docs/architecture/tasks.md` 写死：TaskManager v1 API、key 命名规范、取消语义、错误上报路径。
  - Phase 1 落点（写死）：
    - TaskManager 主实现：`src/gui/services/task_manager.py`（Qt 线程池/信号适配最直接）
    - 任务编排（索引/节流）：`src/gui/services/index_scheduler.py`
  - 选取 3 个高频/高风险任务作为“第一批接入点”（写死）：
    - RAG 索引（全量/增量）
    - Codex 引用检测（节流）
    - AI completion 请求（可取消/超时）
  - 现存并发实现清单（写死，便于替换/收敛）：
    - core 自动索引线程（应移出 core）：`src/core/project.py:173-177`（create_project）/ `src/core/project.py:210-214`（open_project）调用 `_trigger_auto_indexing_async()`
    - completion/AI 请求线程：`src/core/ai_qt_client.py:20` `AIWorkerThread(QThread)`
    - UI 保存后索引线程池：`src/gui/main_window.py:645-736`（`_on_save()`、`_delayed_index_document()`、`_on_document_saved_auto_index()`）

  - key 规范与示例（写死，避免执行期自造导致不可取消/不可去重）：
    - `rag/index/full_scan`
    - `rag/index/doc:{document_id}`
    - `rag/rebuild`
    - `codex/ref_detect/doc:{document_id}`
    - `codex/ref_detect/full_scan`
    - `ai/complete/request:{editor_id}`
    - `ai/complete/stream:{editor_id}`
    - `outline/refresh`
    - `import/project`
    - `export/project`

  - 替换顺序（写死，避免双触发与竞态）：
    1) 先落地 TaskManager + IndexScheduler（但不删除旧路径）
    2) 把 RAG 索引统一切到 IndexScheduler 监听 `Shared.documentSaved`（移除 `MainWindow._on_save()` 内的索引 schedule，见 `src/gui/main_window.py:645-736`）
    3) 移除 core 的 `_trigger_auto_indexing_async()` 与手动 `projectChanged.emit`（与任务 2.1 合并推进）
    4) completion/AI 请求：Phase 1 先保留 `AIWorkerThread`，但把取消与错误上报纳入 TaskManager 统一通道；Phase 2 再把 QThread 迁移到统一 runnable 模型

  **Must NOT do**:
  - 不要在每个模块各自实现线程/取消；必须统一走 TaskManager。

  **Parallelizable**: YES

  **References**:
  - `src/core/ai_qt_client.py`（现有 Qt 异步桥接思路，可作为迁移参考）
  - `src/gui/editor/smart_completion_manager.py`（completion 请求与取消需要统一）
  - `src/core/rag_service.py`（索引/查询会触发重任务）

  **Acceptance Criteria**:
  - [ ] `docs/architecture/tasks.md` 存在并可指导实现（包含 API、key 规范、示例）。
  - [ ] 任何接入点都能在 UI 中触发取消，且取消后不会继续写 DB/写 UI。


### 1. 测试与验证安全网（先让重构可控）

- [ ] 1.1 统一测试入口（保留 unittest，新增 pytest 作为新入口）

  **What to do**:
  - 确保 `python -m unittest discover tests` 可跑（修复路径注入、缺失依赖提示）。
  - 引入 `pytest` + `pytest-qt`：新增最小 smoke 测试（启动主窗口 + 退出）。
  - 依赖落地（写死）：
    - `pytest` 已在 `requirements.txt:8`
    - `pytest-qt` 目前不在 requirements 中 → 作为 dev 依赖加入 `pyproject.toml` 或 `requirements-dev.txt`（与 0.4 对齐）
  - smoke 测试文件位置（写死）：`tests/gui/test_smoke_main_window.py`

  **References**:
  - `tests/` - 现有测试目录
  - `requirements.txt:8` - pytest 已列出
  - `src/gui/main_window.py` - 主窗口创建

  **Acceptance Criteria**:
  - [ ] `python -m unittest discover tests` → PASS
  - [ ] `pytest -q`（至少 1 条 smoke）→ PASS

- [ ] 1.2 Golden Master：为 AI 上下文/提示词/RAG 查询建立固定用例

  **What to do**:
  - 固定一小段中文小说文本 + 一个小型项目树（写死构造方式，避免“内置 demo 在哪？”的不确定）：
    - 通过 `ProjectManager.create_project()` 在临时目录生成一个最小项目（Act/Chapter/Scene + 少量正文）。
    - 或在 `tests/fixtures/` 提供生成脚本（不提交二进制 db）。
  - 对以下模块建立“输入 → 输出”快照测试：
    - 上下文抽取（ContextCollector）
    - Prompt 生成（DynamicPromptGenerator）
    - RAG 查询规划（关键词提取/limit 策略）
  - 快照落点（写死）：
    - 输入：`tests/fixtures/`
    - 期望输出：`tests/golden/`（文本文件）
  - 脱敏规则（写死）：移除时间戳、随机 UUID、绝对路径、API key。
  - deterministic embedder/mock 的接入点（写死，避免测试期猜）：
    - 现状 embedding 生成在 `RAGService.create_embedding_async()`（`src/core/rag_service.py:268-361`）
    - Phase 1（测试优先，最小改动）：让 `RAGService` 支持注入 `embedder`（接口）
      - 默认实现：网络 embedder（保留现有逻辑）
      - 测试实现：`FakeEmbedder`（例如用 `sha256(text)` 生成固定维度向量；不依赖网络/模型）
    - Golden Master 与 overlap 指标默认用 `FakeEmbedder` 跑；真实网络 embedding 仅作为手工/集成验证，不进入 CI 基线。

  **References**:
  - `src/gui/ai/enhanced_ai_manager.py:38`（IntelligentContextBuilder）
  - `src/gui/ai/enhanced_ai_manager.py:417`（DynamicPromptGenerator）
  - `src/core/rag_service.py`（search/rerank 逻辑）
  - `src/core/rag_service.py:268-361`（embedding 生成现状；需要可注入以支持 deterministic 测试）
  - `src/core/sqlite_vector_store.py:280`（ultra_fast fallback 路径）

  **Acceptance Criteria**:
  - [ ] 关键输入的输出稳定（脱敏后 diff 为 0）。
  - [ ] RAG 结果回归标准（写死、可测）：
    - 固定查询集：至少 10 条查询（中文短句 + 人名/地名 + 复合问题各一类），存于 `tests/fixtures/rag_queries.txt`
    - topN：N=10
    - 指标：对每条查询，比较“文档/片段 ID 的集合”（忽略排序），overlap = |A∩B|/|A|，要求平均 overlap ≥ 0.8
    - 约束：测试默认必须使用 deterministic embedder（mock/fake，禁用真实网络 embedding），否则回归值不可重复

### 2. 分层与依赖方向（先立规矩，再拆模块）

- [ ] 2.1 清理 core ↔ gui 耦合：把 `QThread` 自动索引从 `src/core/project.py` 移出

  **Problem**:
  - `src/core/project.py` 内部直接定义并启动 `QThread`（`_trigger_auto_indexing_async`），导致 core 无法脱离 Qt。

  **What to do**:
  - 移除 `src/core/project.py` 中 `_trigger_auto_indexing_async()` 及其所有调用点：
    - 当前调用点：`src/core/project.py:177`（create_project）、`src/core/project.py:214`（open_project）
  - 以事件驱动替代，并写死“唯一真相点”（避免双触发）：
    - **projectChanged 的唯一来源（写死）**：只允许通过设置 `Shared.current_project_path` 触发（setter 内部会 emit，见 `src/core/shared.py:54-61`）。
      - 因此必须移除 `ProjectManager.create_project/open_project` 中的手动 `self._shared.projectChanged.emit(...)`：
        - `src/core/project.py:173-175`（create_project 内的手动 emit）
        - `src/core/project.py:210-212`（open_project 内的手动 emit）
      - **类型一致性（写死）**：`Shared.current_project_path` 的存储类型应保持为 `Path`（现状注解为 `Optional[Path]`，见 `src/core/shared.py:32`），但当前 `ProjectManager` 传入的是 `str(project_path)`（见 `src/core/project.py:170-171`、`src/core/project.py:207-208`）。
        - Phase 1 约定：在 `Shared.current_project_path` setter 内对 `str` 做 `Path(str)` 归一化；对外 emit 仍使用 `str(path)`。
        - 验收：打开/创建项目后，`shared.current_project_path` 为 `Path` 类型，且 `projectChanged` 信号 payload 为字符串路径。
    - **documentSaved 的唯一来源（写死）**：只允许 `ProjectManager.update_document()` 在成功持久化后 emit（现状在 `src/core/project.py:331-369` 的 `self._shared.documentSaved.emit(...)`）。
    - `EnhancedAIManager`（或新增 `gui/services/index_scheduler.py`）监听：
      - `Shared.projectChanged`：初始化/切换 vectors.db（见 4.2）+ 触发一次“未索引文档”扫描
      - `Shared.documentSaved`：对单文档增量索引（debounce 2s）
  - 去重策略（写死）：
    - 每次 projectChanged 只允许同时存在 1 个“全量扫描/补索引”任务；新触发则取消旧任务。
    - documentSaved 的索引任务按 document_id 合并（同一文档短时间多次保存只索引最后一次）。
  - 清理重复触发（写死）：
    - 当前重复触发属实：`src/gui/main_window.py:645-710`（保存后 schedule `_delayed_index_document`）+ `src/gui/main_window.py:722-735`（`_on_document_saved_auto_index`） 与 `src/core/project.py:331-369` 的 `Shared.documentSaved` 会重复触发。
    - 写死保留方案：索引只允许由“监听 `Shared.documentSaved` 的 IndexScheduler”触发；主窗口 `_on_save()` 不再直接 schedule 索引；`_on_document_saved_auto_index()` 改为删除或仅做转发到 IndexScheduler。

  **References**:
  - `src/core/project.py:556`（_trigger_auto_indexing_async）
  - `src/gui/main_window.py`（文档保存后索引也存在）

  **Acceptance Criteria**:
  - [ ] 本任务范围内，`src/core/project.py` 不再 import PyQt6，且不再启动/依赖 `QThread`（核心耦合点先切断）。
  - [ ] “core 全部去 Qt 化”作为后续里程碑，由 5.1（Codex 去 Qt 化）等任务逐步承接，不在本任务强制一次性清零。
  - [ ] UI 仍可在打开项目后自动索引（功能不丢）。
  - [ ] 验证：打开项目后 5 秒内 UI 不冻结；索引任务可取消；重复打开/切换项目不会启动多个并行索引。

- [ ] 2.2 Shared “上帝对象”拆解路线图（不一次性移除）

  **What to do**:
  - 识别 Shared 当前承载的职责（状态、信号、服务定位）。
  - 建立明确的替代：
    - `AppServices`（依赖注入容器/服务注册表）
    - `AppEvents`（统一信号/事件总线）
  - 逐步把字段从 Shared 迁移到更窄的接口。

  **References**:
  - `src/core/shared.py`（Shared 定义）
  - `src/main.py`（注入点）
  - `src/gui/main_window.py`（大量使用）

  **Acceptance Criteria**:
  - [ ] 新增模块边界文档（`docs/architecture/boundaries.md`）。

- [ ] 2.3 Project 持久化边界与“增量更新”的最小可交付路线

  **Goal（写死）**:
  - 让“增量更新”有可执行含义：对单文档编辑/重命名/移动/排序/删除，不必每次 `DELETE FROM documents` 全量重写。
  - 但也不强迫 Phase 1 立刻改掉所有写入（先把接口边界抽出来，保证可回滚）。

  **What to do**:
  - 定义 `ProjectRepository`（application/infrastructure 边界）最小 API（写死）：
    - `load_project()` → metadata + documents
    - `save_project_full(data)`（Phase 1：复用当前全量写入）
    - `upsert_document(doc)`（Phase 2：单文档增量）
    - `delete_document(doc_id)` / `move_document(doc_id, new_parent_id, new_order)` / `reorder_children(parent_id, ordered_ids)`
    - `transaction()`：显式事务边界
  - 增量路线（写死）：
    - Phase 1：保留 `save_project_full()`（仍可全量重写），但把调用点收敛到 repository（避免散落直接调用 `DatabaseManager.save_project_data()`）
    - Phase 2：实现并接入 `upsert_document` 等增量 API；编辑器“保存单文档”必须走增量
    - Phase 3：仅在“结构性变更”（批量导入/全树重排）才允许全量写入
  - Outline 关联（写死）：
    - Phase 1：大纲刷新改为 debounce + worker 计算 + 主线程 apply（先止血 UI 卡顿）
    - Phase 2：引入 OutlineModel（建议 QAbstractItemModel），对 rename/move/reorder 走增量更新（不全量重建）

  **References**:
  - `src/core/database_manager.py:203-239`（当前全量写入实现，作为 Phase 1 的“已知行为”）
  - `src/core/project.py`（保存/删除/移动等业务入口与调用链）
  - `src/gui/panels/outline_panel.py`（大纲刷新触发与 UI 更新路径）

  **Acceptance Criteria**:
  - [ ] Phase 1：所有项目依然能正常保存/加载（行为不回归），且持久化调用点集中到 repository（可审计）。
  - [ ] Phase 2：单文档保存不再执行 `DELETE FROM documents`（可通过 debug 日志或 instrumentation 验证）。
  - [ ] Phase 2：rename/move/reorder 在大纲中可见且不触发全量重建（至少通过“刷新耗时显著下降 + 日志标记”验证）。

### 3. AI 客户端与补全编排重构（先契约化再拆）

- [ ] 3.1 拆分 `src/core/ai_client.py` 为 provider 策略包

  **What to do**:
  - 把 provider 相关逻辑拆成独立模块（OpenAI/Claude/Gemini/Ollama/Custom）。
  - 引入统一接口：`LLMProvider.complete()` / `complete_stream()` / `supports_tools()`。
  - 统一错误类型（429/5xx/timeout）与重试策略（在一处实现）。

  **References**:
  - `src/core/ai_client.py`（目前“全都在一个文件里”）
  - `src/core/secure_key_manager.py`（密钥来源）

  **Acceptance Criteria**:
  - [ ] 不改 UI 前提下，所有 provider 仍可完成一次请求（可用 mock）。

- [ ] 3.2 `EnhancedAIManager` 变薄：上下文/提示词/网络请求/渲染分层

  **What to do**:
  - `IntelligentContextBuilder`、`DynamicPromptGenerator` 抽到 application 层，可被测试。
  - GUI 层只负责：触发、显示状态、把结果交给编辑器。

  **References**:
  - `src/gui/ai/enhanced_ai_manager.py:38`（IntelligentContextBuilder）
  - `src/gui/ai/enhanced_ai_manager.py:417`（DynamicPromptGenerator）
  - `src/gui/ai/enhanced_ai_manager.py:550`（EnhancedAIManager）
  - `src/gui/editor/smart_completion_manager.py`（展示/应用策略）

  **Acceptance Criteria**:
  - [ ] Context/Prompt 逻辑有单测或 golden master。

### 4. RAG/向量存储重构（性能与分层纠偏）

- [ ] 4.1 让 `SQLiteVectorStore` 回归“纯存储/纯检索”

  **Problem**:
  - `src/core/sqlite_vector_store.py:280` 的 `similarity_search_ultra_fast` 包含：jieba 分词、AI 关键词提取、复杂 fallback 与 debug 日志。

  **What to do**:
  - 把“关键词提取/查询规划”搬到 application 层（例如 `RAGQueryPlanner`）。
  - `SQLiteVectorStore` 只接受“query embedding”或“SQL LIKE query tokens”并返回候选 chunks。
  - 降低日志噪音（移除 `logger.critical` 的 debug emoji）。

  **References**:
  - `src/core/sqlite_vector_store.py:280`（ultra fast path）
  - `src/core/rag_service.py`（RAG orchestration）

  **Acceptance Criteria**:
  - [ ] 大项目下不出现 UI 卡死：以 `9.1` 的 tick jitter 指标为准（idle/轻交互 max ≤ 120ms；重操作必须显示进度/可取消）。
  - [ ] 对 medium 基准项目，执行 10 次固定查询的检索耗时写入 `perf.log`，并在 `docs/perf/current.json` 记录统计值（p95 不超过 `9.1` 对应预算）。
  - [ ] RAG 查询结果在 golden master 上不过度退化：按 `1.2` overlap 指标验收。

- [ ] 4.2 明确 embedding 兼容与重建策略

  **What to do**:
  - 在 `vectors.db` 记录：embedding_model、维度、chunk_size/overlap 版本。
  - **项目隔离（必须）**：解决当前 embeddings “跨项目混用/泄露”的结构性风险：
    - 默认方案 A（推荐）：每个项目独立 vectors 库，放在项目目录（例如 `<project_path>/.rag/vectors.db`）。
    - 备选方案 B：在 `document_embeddings` 表新增 `project_id` 列，并对所有写入/查询强制 `WHERE project_id = ?` 过滤。
  - 迁移策略（默认）：
    - **现状（写死）**：当前 vectors.db 在启动时被硬编码为全局路径 `~/.ai-novel-editor/vector_store/vectors.db`（见 `src/main.py:94-99`）。
    - **目标（写死）**：打开项目后，vectors 路径应解析为：`<project_path>/.rag/vectors.db`（目录不存在则创建）。
  - **首次切换策略（写死）**：若检测到“目标 per-project vectors.db 不存在”且“旧全局库存在”，弹窗提供 3 个选项：
      - 选项 A（默认/推荐）：重建索引（可靠；不会错误迁移别的项目内容）
      - 选项 B：暂不重建、并禁用 RAG（保持可用性但不做检索；避免跨项目泄露）
      - 选项 C：尝试迁移（仅当能可靠识别 project 归属时才允许；否则按钮置灰并提示原因）
    - 默认优先重建（更可靠），迁移只作为“可证明安全”的优化。
  - **模式门槛（写死，避免执行期分叉）**：
    - Phase 1 强制走方案 A（per-project vectors.db）
    - 选项 B 仅允许作为“暂不重建并禁用 RAG”的安全退路（不允许继续用旧全局库做检索）
    - 选项 C 只有在方案 B（表内 project_id 过滤）落地后才允许启用

  - **触发位置（写死，避免实现期猜）**：
    - `ProjectManager.create_project/open_project` 成功后只做一件事：设置 `Shared.current_project_path = project_path`（由 `src/core/shared.py:54-61` setter 统一 emit `projectChanged`）
    - `ProjectController`（或 `gui/services/index_scheduler.py`）监听 `Shared.projectChanged` 事件，执行：
      1) 解析 per-project vectors 路径
      2) 检测旧全局库与新库存在性
      3) 必要时弹窗询问（A/B/C）
      4) 根据选择设置 `shared.vector_store` 与 `shared.rag_service` 的工作模式（enabled/disabled + rebuild task）

  - **对象生命周期与传播路径（写死，避免“切换了但旧对象还在用老引用”）**：
    - 现状：`IntelligentContextBuilder.__init__()` 会把 `shared.rag_service` 缓存到 `self.rag_service`（`src/gui/ai/enhanced_ai_manager.py:41-52`）。
    - Phase 1 默认策略（写死）：RAGService 实例保持稳定，只替换其内部的 vector_store：
      1) `new_store = SQLiteVectorStore(<project_path>/.rag/vectors.db)`
      2) `shared.vector_store = new_store`
      3) 若 `shared.rag_service` 存在：调用 `shared.rag_service.set_vector_store(new_store)`（避免新建实例导致旧引用失效）
      4) 同步刷新已创建对象的缓存引用：在 `EnhancedAIManager` 的 `projectChanged` handler 中执行 `context_builder.rag_service = shared.rag_service`（以及需要的话更新 `codex_manager/reference_detector` 缓存）
    - 若 `shared.rag_service` 不存在（ImportError/禁用）：只设置 `shared.vector_store`，并保证 UI 清晰显示“RAG 未启用”。
  - 如果不兼容：提示用户重建索引，并在后台可取消。

  **References**:
  - `src/core/sqlite_vector_store.py`（rag_config 表）
  - `src/main.py:94-99`（当前 vectors.db 的创建位置；将作为迁移前的事实来源）
  - `src/core/shared.py`（项目切换事件源；用于触发 vectors 路径重解析）

  **Acceptance Criteria**:
  - [ ] 旧项目打开时可以检测并提示，不会静默失败。
  - [ ] 不会出现跨项目检索到其它项目内容的情况（隐私护栏）。
  - [ ] 新建两个测试项目 A/B（不同内容），分别建立索引后，对相同查询执行检索：A 的结果只来自 A，B 的结果只来自 B（可用 doc_id 前缀/路径标记验证）。

  **Must NOT do**:
  - 不允许“继续用旧全局库做检索但又无法按 project 过滤”的模式（会违反隐私护栏）。

### 5. Codex 重构（核心数据结构 + UI 解耦）

- [ ] 5.1 CodexManager 去 Qt 化：Domain + Repository + Qt Adapter

  **What to do**:
  - 把 `CodexEntry`/`CodexReference` 等数据结构迁移到 domain。
  - 把数据库交互封装到 repository。
  - GUI 需要信号时，用 `QtCodexAdapter(QObject)` 转发 domain 事件。

  **References**:
  - `src/core/codex_manager.py`（目前在 core 里 try import PyQt6 并自建 stub）
  - `src/core/database_manager.py`（codex tables）
  - `src/core/reference_detector.py`（引用检测逻辑）

  **Acceptance Criteria**:
  - [ ] core/domain 层不依赖 PyQt6：至少 `python -c "import core.codex_manager"` 不应引入 PyQt6（或在迁移后该模块被替换为不含 Qt 的实现）。
  - [ ] CodexPanel 功能不回归：Top10 工作流脚本中的 Codex 场景逐条 PASS（见 0.3）。

### 6. UI 巨型模块拆解（Outline/MainWindow/CodexPanel）

- [ ] 6.1 拆解 `MainWindow`（把 2000 行切成可维护的子模块）

  **What to do**:
  - 按职责拆分：layout、menu/toolbar、dialogs、AI wiring、Codex wiring、state restore。
  - 提供清晰的“初始化顺序图”和“依赖注入点”。

  **References**:
  - `src/gui/main_window.py`
  - `src/main.py`（依赖注入现状）

  **Acceptance Criteria**:
  - [ ] 启动逻辑不变；重构后仍能打开窗口并正常操作。

- [ ] 6.2 拆解 `OutlinePanel`，并引入刷新/虚拟化策略

  **What to do**:
  - 把统计/预览/树构建/动作处理拆成独立组件。
  - 对大项目：避免每次全量重建树；采用增量更新或延迟刷新。

  **References**:
  - `src/gui/panels/outline_panel.py`
  - `src/core/project.py`（文档树来源）

  **Acceptance Criteria**:
  - [ ] 大纲刷新在大项目下不超过性能预算：`docs/perf/current.json` 中 `outline_refresh` 对应指标满足 `9.1`。
  - [ ] 重命名/移动/排序的增量更新行为可观测：至少在 debug 日志中标记“incremental update”路径被触发（避免暗中全量重建）。

- [ ] 6.3 CodexPanel 组件库化

  **What to do**:
  - 把卡片视图、列表视图、统计视图拆分成子组件。
  - 统一搜索/过滤控件样式与行为。

  **References**:
  - `src/gui/panels/codex_panel.py`
  - `src/gui/panels/modern_codex_card.py`

  **Acceptance Criteria**:
  - [ ] Codex UI 风格一致：按 `docs/ui/tokens.md`（执行阶段新增）中的 spacing/font/radius 规则核对 5 个关键控件（搜索框、卡片、列表项、按钮、空状态）。
  - [ ] 交互不回归：Top10 工作流脚本中的 Codex 场景逐条 PASS（见 0.3）。

### 7. Editor/Completion 系统收敛（Ghost/Inline/Popup）

- [ ] 7.1 明确“唯一补全系统”与状态机

  **What to do**:
  - 盘点：`src/gui/editor/` 下多种 completion/ghost 实现（deep/optimal/inline/widget）。
  - **默认取舍（写死，避免执行者拍脑袋）**：
    - 保留并作为入口：`src/gui/editor/smart_completion_manager.py`（协调器）
    - 作为默认 ghost 渲染：优先 `src/gui/editor/optimal_ghost_text.py`（体量更小，便于收敛与测试）
    - 保留 inline：`src/gui/editor/inline_completion.py`
    - 保留 popup：`src/gui/editor/completion_widget.py`，但建议重命名为 `completion_popup.py`（避免与 AI settings 冲突）
    - 逐步废弃：`src/gui/editor/deep_integrated_ghost_text.py`（先做功能对齐/回归用例，再删除）
    - AI 配置侧的 `src/gui/ai/completion_widget.py` 建议重命名为 `completion_settings_widget.py`
  - 抽离纯状态机到 domain/application：
    - 状态：idle → requesting → streaming → suggestion_ready → applied/cancelled
    - 事件：Tab/Shift+Tab/Esc/文本变更/光标移动/超时/网络错误
  - UI 展示（ghost/inline/popup）变成可替换渲染器（renderer interface），由协调器驱动。

  **References**:
  - `src/gui/editor/smart_completion_manager.py`（单一入口类）
  - `src/gui/editor/text_editor.py`（键盘处理与展示）
  - `src/gui/editor/deep_integrated_ghost_text.py`
  - `src/gui/editor/optimal_ghost_text.py`
  - `src/gui/editor/inline_completion.py`
  - `src/gui/ai/completion_widget.py` 与 `src/gui/editor/completion_widget.py`（同名冲突风险）

  **Acceptance Criteria**:
  - [ ] 手动模式：Tab 触发 → Tab 应用（README 描述）稳定。
  - [ ] 自动模式：延迟触发可控，不连发。
  - [ ] 取消/超时/错误时 UI 不残留“黄色条/幽灵文本”。
  - [ ] 覆盖现有回归测试：`tests/test_ghost_text_*`、`tests/test_tab_esc_ghost_text.py`、`tests/test_esc_key_handling.py`（执行阶段如有失配需修复测试与实现的一致性）。

### 8. 导入导出重构（拆分格式适配 + 可靠性）

- [ ] 8.1 拆分 `import_export_engine.py` 为格式插件

  **What to do**:
  - 把 TEXT/MD/DOCX/PDF/HTML 各自实现拆开；统一接口：`Importer`/`Exporter`。
  - 为每种格式提供最小 round-trip 用例。

  **References**:
  - `src/core/import_export_engine.py`
  - `src/core/import_manager.py`
  - `src/core/export_manager.py`
  - `requirements.txt`（python_docx、weasyprint 等）

  **Acceptance Criteria**:
  - [ ] 至少 1 个示例项目可以导出再导入（结构/正文不丢）：以 `tools.generate_benchmark_project --size small` 的项目为基准，执行导出→新目录导入→比对文档树与正文（允许 metadata 时间戳差异）。
  - [ ] 新增最小自动化回归：`pytest -q` 中包含 1 条 import/export round-trip 测试（执行阶段实现测试文件）。

### 9. 性能预算（必须写死并在执行时测量）

- [ ] 9.1 定义并记录性能基线

  **What to do**:
  - 定义 3 个基准数据集（写死，便于复现）：
    - small：~50 文档 / 总正文 ~50k 字
    - medium：~300 文档 / 总正文 ~300k 字
    - large：~1000 文档 / 总正文 ~1.5M 字
  - 对每个数据集测量 4 类指标（写死）：
    - cold start：进程启动 → 主窗口 show 完成
    - open project：点击打开项目 → 左侧项目树与中间编辑器可交互
    - outline refresh：触发一次大纲刷新 → 刷新完成
    - AI completion（mock）：触发补全 → UI 出现 suggestion（ghost/inline/popup 任一）
  - **UI 冻结预算（写死，可测）**：用 60Hz `QTimer` 监控 tick jitter，记录最大间隔：
    - idle/轻交互：最大 tick 间隔 ≤ 120ms
    - 允许重操作：如触发索引/导入/重建，可超过 120ms 但必须展示进度/可取消，并记录在日志中
  - **预算（默认目标，写死）**（先记录现状 baseline，再逐步压缩）：
    - cold start：small ≤ 2.0s；medium ≤ 3.5s；large ≤ 6.0s
    - open project：small ≤ 1.0s；medium ≤ 2.5s；large ≤ 5.0s
    - outline refresh：small ≤ 150ms；medium ≤ 400ms；large ≤ 900ms
    - AI completion（mock）：small/medium/large ≤ 800ms
  - 记录方式（写死）：
    - 运行时输出 perf 日志（JSON lines）到 `~/.ai-novel-editor/logs/perf.log`
    - 在 repo 中维护 `docs/perf/baseline.json` 与 `docs/perf/current.json`（只记录统计值与机器信息摘要，不记录用户文本内容）

  - 基准数据集生成方式（写死到命令级，执行阶段实现对应工具）：
    - 生成命令（建议，Phase 1 写死落点为 repo 根目录 `tools/` python 包）：
      - `python -m tools.generate_benchmark_project --size small --out tests/fixtures/projects/small`
      - `python -m tools.generate_benchmark_project --size medium --out tests/fixtures/projects/medium`
      - `python -m tools.generate_benchmark_project --size large --out tests/fixtures/projects/large`
    - 输出约束（写死）：生成的 fixture 必须是“可打开的项目目录”（包含 `project.db` 与最小资源），且不含真实用户正文（全为合成文本）。
    - golden 更新规则（写死，Phase 1 写死落点为 repo 根目录 `tools/` python 包）：
      - 允许新增/更新 golden 仅通过显式命令：`python -m tools.update_golden --all`
      - 默认 CI 不更新 golden，只做 diff 校验。

  - 埋点位置清单（写死，避免执行期在调用链里猜）：
    - cold start：
      - start：`src/main.py:66`（进入 `main()`）
      - end：`src/main.py:235`（`main_window.show()` 之后）
    - open project：
      - start：`src/gui/controllers/project_controller.py:52`（`on_open_project()` 进入）
      - end：`src/gui/controllers/project_controller.py:70`（`project_structure_changed.emit()` 之后，且 UI 可交互）
    - outline refresh：
      - start/end：在 `src/gui/panels/outline_panel.py` 的“刷新入口函数”前后打点（执行阶段定位具体方法名）
    - AI completion（mock）：
      - start/end：在 `src/gui/editor/smart_completion_manager.py` 的“发起 completion”与“渲染 suggestion”前后打点（执行阶段定位具体方法名）

  **Acceptance Criteria**:
  - [ ] `docs/perf/baseline.json` 存在，包含 small/medium/large 的 4 类指标数值。
  - [ ] 任意一次重构里程碑后更新 `docs/perf/current.json`，并能说明“变快/变慢”的原因。
  - [ ] 若某指标超预算：必须有降级方案（例如关闭某自动刷新/索引节流/延迟加载 Codex）并可通过配置开关启用。

### 10. 打包与发布（强烈建议纳入，否则重构价值很难落地）

- [ ] 10.1 明确打包目标与运行模式（默认：PyInstaller + Windows）

  **Decision (default)**:
  - Windows 优先；打包工具默认 PyInstaller。
  - 运行模式默认“安装版 + 可选便携版（portable）”。

  **What to do**:
  - 修复仓库规则冲突：`.gitignore` 当前忽略 `*.spec`（`.gitignore:210`），而 PyInstaller 需要 spec 文件。
    - 方案：给 spec 做白名单（例如 `!pyinstaller/ai_novel_editor.spec`）或将 spec 放到不被忽略的路径/命名。
  - 补齐打包所需资源清单：Qt plugins、图标、QSS、jieba 词典、weasyprint 依赖。
  - 建立 `scripts/build.ps1`（或同等脚本）+ `pyinstaller.spec`（执行阶段实现）。
  - 明确数据目录策略：
    - portable：相对路径（项目旁边）
    - installed：`QStandardPaths`（当前已使用）

  **References**:
  - `src/main.py`（图标路径与启动）
  - `src/gui/themes/theme_manager.py` + `src/resources/styles/*.qss`（资源加载）
  - `requirements.txt`（weasyprint/jieba 等）

  **Acceptance Criteria**:
  - [ ] spec 文件可被 git 跟踪（不再被 ignore）。
  - [ ] 打包后可启动并打开项目。
  - [ ] 主题样式、图标、导入导出至少 TEXT/MD 工作。

- [ ] 10.2 为打包做依赖风险分级（尤其 weasyprint）

  **What to do**:
  - 识别“难打包依赖”：weasyprint、cryptography、nltk 数据等。
  - 为每个风险点提供降级策略（例如 PDF 导出不可用时给出清晰提示）。

  **Acceptance Criteria**:
  - [ ] 打包版不会因为缺少系统依赖而直接崩溃：在缺少 weasyprint 依赖的环境下启动仍成功，尝试 PDF 导出时给出明确降级提示（功能不可用原因 + 解决方式）。

### 11. 安全与隐私（必须）

- [ ] 11.1 密钥存储审计与加固

  **What to do**:
  - 审计 `src/core/secure_key_manager.py`：加密算法、密钥来源、错误处理。
  - 明确策略：
    - 默认使用安全存储（OS keychain 或本地加密文件）
    - 配置导出时永不包含密钥

  **References**:
  - `src/core/config.py`（迁移 api_key 到安全存储）
  - `src/core/ai_client.py`（读取密钥）
  - `src/core/rag_service.py`（读取密钥）

  **Acceptance Criteria**:
  - [ ] 任何日志/导出文件不包含明文 API key。
  - [ ] 重点审计并修复：`src/main.py:116` 的 RAG 配置日志输出（`logger.info(f"RAG配置: {rag_config}")`）必须改为脱敏输出。
  - [ ] 验收脚本：启动一次应用后，检查日志目录 `~/.ai-novel-editor/logs/`（见 `src/main.py`）中不出现以下模式：
    - `api_key`、`sk-`、`Bearer `、`x-api-key`、`x-goog-api-key`

- [ ] 11.2 Prompt 注入与数据泄露防护（最小可行）

  **What to do**:
  - 在 Prompt 组装时对来源做边界标记：
    - 用户输入（user）
    - RAG 片段（quoted context）
    - Codex（structured facts）
  - 默认禁用“工具调用”或仅在明确开关启用时开放。

  **Acceptance Criteria**:
  - [ ] RAG 文档中的指令不会被当作系统指令执行：新增 1 条单测/快照用例，输入包含“忽略上文/泄露密钥”的 RAG 片段，验证最终 prompt 中该片段被明确包裹为引用/上下文而非 system 指令。

### 12. 配置系统与迁移（可维护性关键）

- [ ] 12.1 配置 schema 化与迁移（避免 config.json 越堆越乱）

  **What to do**:
  - 定义 `ConfigSchema`（字段、默认值、版本号）。
  - 实现 config 迁移：旧字段兼容 + 清理废弃字段。
  - 统一“配置写入频率”：避免 UI 每次改动都落盘。

  **References**:
  - `src/core/config.py`（当前默认配置与迁移 api_key）
  - `src/gui/ai/config_mapper.py`（UI 映射）

  **Acceptance Criteria**:
  - [ ] config 版本可追踪；旧配置升级后仍可正常启动：提供 1 份旧配置 fixture（`tests/fixtures/config_v0.json`，执行阶段新增），运行一次迁移并启动 `python src/main.py` 通过。

### 13. 文档与开发者体验（降低二次屎山概率）

- [ ] 13.1 增补开发文档（运行/测试/打包/架构）

  **What to do**:
  - `docs/architecture/`：边界、数据流、任务系统。
  - `docs/engineering/`：规范、提交策略、调试指南。
  - `docs/ui/`：设计 tokens、组件库规范。

  **Acceptance Criteria**:
  - [ ] 文档中列出的命令可直接复制运行：
    - `pip install -r requirements.txt`
    - `python -m unittest discover tests`
    - `python src/main.py`
    - `pytest -q`（如启用 pytest/pytest-qt）
    并在文档中注明预期结果（PASS/启动成功/窗口出现）。

---

## Commit Strategy（建议）
- 每完成一个可验证任务提交一次，避免“巨型一次性提交”。
- 优先顺序：仓库卫生 → 测试安全网 → 分层纠偏 → 拆巨型模块 → UI 收敛/优化。

---

## Success Criteria（最终成功标准）

### Verification Commands（建议执行者在每个里程碑跑）
```bash
python -m unittest discover tests
python src/main.py
```

### Final Checklist
- [ ] 任何重构都不破坏项目数据可用性（能开/能保存/能重开）。
- [ ] AI/RAG/Codex 可以在“无真实 API key”情况下运行（mock 模式）用于测试与演示。
- [ ] 关键模块不再是 2k 行巨文件；职责清晰。
- [ ] UI 风格一致、状态反馈集中、卡死/残影问题显著减少。
