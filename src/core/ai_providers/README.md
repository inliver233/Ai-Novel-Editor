Provider strategy modules for AIClient.

- `openai.py`: OpenAI-compatible provider (reasoning model support).
- `claude.py`: Anthropic Claude provider.
- `gemini.py`: Google Gemini provider (thinking mode support).
- `ollama.py`: Ollama OpenAI-compatible provider.
- `custom.py`: Custom OpenAI-compatible provider with endpoint overrides.
