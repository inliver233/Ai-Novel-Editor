---
mode: plan
task: UI + AI follow-up fixes
created_at: "2026-02-10T05:57:49+08:00"
complexity: medium
---

# Plan: UI + AI follow-up fixes

## Goal
- Allow configuring `max_tokens` up to at least 1,000,000 in the AI config UI without input truncation.
- Prevent auto AI completion / RAG retrieval from injecting unrelated old-project content when creating a new empty project/document.
- Fix bottom status UI (status bar / editor panel status frame) so it does not clip or visually overlap at common DPI/font sizes.
- Close all issues in the batch CSV with repeatable verification, then run regression and mark `Regression_Status=DONE`.

## Scope
- In:
  - AI config dialog: `max_tokens` input range + persistence.
  - Auto-completion guards for empty/short context and “no project open” state.
  - RAG retrieval safeguards for empty/placeholder queries.
  - Status bar sizing/layout adjustments to avoid clipping/overlap.
  - Add/adjust targeted unit/Qt tests.
- Out:
  - Provider/model-specific max output token discovery.
  - Major UI redesign beyond layout fixes.

## Assumptions / Dependencies
- Use repo venv: `.tmp\\ane0305-venv-311\\Scripts\\python.exe`.
- `pytest-qt` is available for lightweight Qt widget tests.
- No network access is required for tests (RAG/AI calls are stubbed).

## Phases
1. Generate batch Issue CSV + this plan (timestamp/slug matched).
2. Fix `max_tokens` UI upper limit and add a Qt test.
3. Fix empty-project auto completion + RAG leakage guards and add unit/Qt tests.
4. Fix status bar / bottom UI clipping/overlap and add a Qt layout test.
5. Run regression (`compileall` + `pytest`) and close `Regression_Status` via a meta CSV-only commit.

## Tests & Verification
- `B1` -> `python -m pytest -q tests/test_ai_config_max_tokens.py`
- `B2` -> `python -m pytest -q tests/test_empty_project_completion_guards.py`
- `B3` -> `python -m pytest -q tests/test_status_bar_layout.py`
- Regression -> `python -m compileall -q src` + `python -m pytest -q`

## Issue CSV
- Path: issues/2026-02-10_05-57-06-ui-ai-fixes-v1.csv
- Must share the same timestamp/slug as this plan.

## Tools / MCP
- none

## Acceptance Checklist
- [ ] `max_tokens` field accepts 1,000,000+ and persists.
- [ ] New empty project/document does not auto-trigger AI completion and does not retrieve unrelated prior content.
- [ ] Bottom status UI does not clip/overlap at default and elevated DPI font sizes (verified by Qt geometry test).
- [ ] All issues `Dev_Status/Review1_Status/Regression_Status` are `DONE`.

## Risks / Blockers
- Qt geometry tests can be sensitive to platform fonts/DPI; keep assertions tolerant and focused on overlap/clipping.

## Rollback / Recovery
- Revert the per-issue commit(s) for the affected subsystem.

## Checkpoints
- Commit after each Issue row; meta commit after regression.

## References
- `src/gui/ai/unified_ai_config_dialog.py`
- `src/core/shared.py`
- `src/gui/services/index_scheduler.py`
- `src/application/ai_context.py`
- `src/gui/ai/enhanced_ai_manager.py`
- `src/gui/status/status_bar.py`
- `src/gui/editor/editor_panel.py`
